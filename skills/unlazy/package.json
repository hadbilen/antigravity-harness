{
  "name": "unlazy-skill",
  "version": "2.1.0",
  "private": true,
  "description": "Zero-dependency enforcement tools for the unlazy skill (vendored without the upstream regression suite)",
  "type": "module",
  "scripts": {
    "check": "node scripts/gate-lint.mjs --help"
  },
  "engines": {
    "node": ">=16"
  }
}
